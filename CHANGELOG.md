# Changelog

All notable changes to `@absolutejs/auth`.

This file is generated by `absolute-changelog` from the entries in
`changelog/`. Edit an entry, not this file — and add new ones under
`changelog/unreleased/`.

## 0.94.0 — 2026-09-26

### Added

- **OAuth callbacks accept response_mode=form_post (Sign in with Apple): a POST to the callback route is re-issued as a same-site GET carrying only code, state, error, error_description and user, so SameSite=Lax state cookies are sent and the normal callback runs.** (`createAuthApplications`)

## 0.93.0 — 2026-09-26

### Added

- **Optional persistent session cookies: `persistentCookie` on `instantiateUserSession` and `promoteToSession`, and `persistentSessionCookie` on the `webauthn` block, give the session cookie a Max-Age equal to the session length so a sign-in survives browser restarts. Without them the cookie still ends with the browser.** (`InsantiateUserSessionProps`, `instantiateUserSession`, `WebAuthnConfig`, `webauthnRoutes`, `getUserSessionId`)

## 0.92.0 — 2026-09-26

### Added

- **Passkey management: `GET`, `PATCH` (rename) and `DELETE` on `/auth/webauthn/credentials` for the signed-in user's own passkeys. Passkeys now have a `name` (webauthn migration 0003): the one passed at registration, or the provider's name from its AAGUID (iCloud Keychain, Google Password Manager, Windows Hello, 1Password…). `hasOtherSignInMethod` can protect the last passkey. The client gains `passkeys.rename`, a working `passkeys.list`, `runPasskeyRegistration(client, { name })` and `runPasskeyAuthentication` for a sign-in button.** (`AuthClientRoutes`, `PasskeySummary`, `WebAuthnConfig`, `WebAuthnCredential`, `WebAuthnCredentialStore`, `WebAuthnRegistrationResult`, `createAuthClient`, `defaultPasskeyName`, `runPasskeyAuthentication`, `runPasskeyRegistration`, `useUpgradeToPasskey`, `webauthnCredentialsTable`, `webauthnRoutes`)
- **Sessions record how and where they signed in: `signInMethod` (provider name, `passkey`, `password`, `magic_link`, `otp`, `sso`) and the User-Agent (sessions migration 0003). `GET /auth/sessions` returns them with a coarse `device` (`{ browser, os }`) via `describeUserAgent`; pass the callback's new `request` to `instantiateUserSession` to record OAuth sign-ins. The client's `sessions.list` now returns the array the server sends.** (`CallbackContext`, `InsantiateUserSessionProps`, `SessionData`, `createAuthClient`, `describeUserAgent`, `instantiateUserSession`, `oidcSsoRoutes`, `sessionRoutes`, `useSessions`)
- **Sign-in identities: several provider accounts can open one user. New `identities` config mounts `GET /auth/identities` and `DELETE /auth/identities/:id` (never removes the last way in unless `hasOtherSignInMethod` says so); `linkCallbackIdentity` links the OAuth callback's account to the signed-in user from `onLinkIdentity` and throws `AuthIdentityConflictError` when another user already owns it, so `onLinkIdentityConflict` finally runs. Postgres/Neon and in-memory stores; migration block `identities` creates `auth_identities` (compatible with existing app tables and the CLI importer) with a unique provider+subject index.** (`AuditEventType`, `AuthConfig`, `AuthIdentity`, `AuthIdentityStore`, `BlockName`, `IdentitiesConfig`, `authIdentitiesTable`, `createAuthApplications`, `createInMemoryIdentityStore`, `createNeonIdentityStore`, `createPostgresIdentityStore`, `identityRoutes`, `linkCallbackIdentity`, `resolveCallbackIdentity`)

## 0.91.0 — 2026-09-26

### Added

- **Invitations accept an optional invitee name and personal note: stored with the invitation (organizations migration 0002), passed to onSendInvitation, and the name is returned when listing invitations. Name is trimmed to 200 characters; the note keeps line breaks and is limited to 2,000.** (`OrganizationInvitation`, `OrganizationInvitationMessage`, `inviteToOrganization`, `organizationInvitationsTable`, `organizationRoutes`, `createAuthApplications`)

## 0.90.0 — 2026-09-25

### Added

- **Add per-grant refresh-token family inventory and revocation: optional `listFamilies`, `getFamily` and `revokeFamily` store methods (in-memory and Postgres/Neon, with a family index migration), and pass the family id to `getAccessTokenClaims` so resource servers can reject access tokens from a revoked device immediately.** (`OidcRefreshTokenFamily`, `OidcRefreshTokenStore`, `OidcProviderConfig`, `createInMemoryOidcRefreshTokenStore`, `createPostgresOidcRefreshTokenStore`, `createNeonOidcRefreshTokenStore`)

## 0.89.0 — 2026-09-24

### Security

- **Bind WebAuthn ceremonies to single-use server records, purpose, user and session; add a durable Postgres challenge store and prevent credential owner/key replacement or counter rollback.** (`createPostgresWebAuthnChallengeStore`, `createInMemoryWebAuthnChallengeStore`, `webauthnChallengesTable`, `WebAuthnChallenge`, `WebAuthnChallengeStore`, `WebAuthnConfig`, `createAuthApplications`, `buildClientProviders`, `resolveProviderClientConfiguration`, `webauthnRoutes`)

## 0.88.0 — 2026-09-24

### Added

- **Add encrypted interactive-transaction OAuth credential coordination; serialize refresh and removal with grant row locks, persist recovery before returning failures, and reject stale or forged credential handles. Bound ambiguous token renewals and require reconnection after permanent failures.** (`createCoordinatedOAuthLinkedProviderCredentialResolver`)

## 0.87.1 — 2026-09-24

### Fixed

- **Declare session and linking cookies on the OAuth authorization route so opt-in session binding accepts existing signed-in users. Add an authorization-start regression test.** (`createAuthApplications`)

## 0.87.0 — 2026-09-24

### Added

- **Add owner-bound encrypted linked-provider grant storage, session-bound OAuth linking, and an exported OAuth authorization resolver for separately consented connectors.** (`createEncryptedLinkedProviderGrantStore`, `resolveOAuthAuthorization`, `AuthConfig`, `createAuthApplications`)

## 0.86.1 — 2026-09-23

### Fixed

- **Allow authorized nonmember organization managers to list memberships. Revoke invitations when their delivery hook fails and return a sanitized retryable error.** (`organizationRoutes`, `createAuthApplications`)

## 0.86.0 — 2026-09-23

### Breaking

- **Bind organization invitation acceptance to a verified email and atomically consume the invitation with membership creation. Deny expired, revoked, missing-organization and suspended-member invitations; preserve existing member roles.** (`acceptInvitation`, `OrganizationsConfig`, `OrganizationStore`, `listUserOrganizations`, `organizationRoutes`)
  _Migration:_ Configure organizations.getVerifiedEmail with provider-verified email evidence. Direct acceptInvitation callers must supply verifiedEmail. Custom OrganizationStore adapters must implement atomic acceptInvitation; older adapters fail closed.

## 0.85.0 — 2026-09-23

### Breaking

- **Use the native Citra Neon provider for Auth configuration and linked-account refresh** (`createNeonProviderConfiguration`, `neonProviderConfiguration`)
  _Migration:_ Move createNeonProviderConfiguration results from customProviders.neon to providersConfiguration.neon. Neon is now a built-in Citra provider; credentials, scopes and offlineAccess inputs are unchanged.

### Changed

- **Include Neon in the typed built-in provider registry and configuration resolution** (`buildClientProviders`, `resolveProviderClientConfiguration`)

## 0.84.0 — 2026-09-23

### Added

- **Add a Neon partner OAuth preset with S256 PKCE, explicit management scopes and opt-in offline access** (`createNeonProviderConfiguration`, `neonProviderConfiguration`, `NeonManagementScope`, `NeonProviderOptions`)

## 0.83.0 — 2026-09-23

### Added

- **Provide a Bun-native PostgreSQL migration entry point with transaction rollback, concurrency serialization and connection cleanup** (`runBunMigrations`, `BunMigrationsOptions`)

### Changed

- **Regenerate createAuthApplications and oidcProviderRoutes declarations with equivalent property ordering; accepted inputs and runtime behavior are unchanged** (`createAuthApplications`, `oidcProviderRoutes`)

## 0.82.0 — 2026-09-22

### Added

- **Expose a fixed-path sign-out subapp for directly inferred Eden clients and document Elysia 2 schema argument order** (`createSignoutApi`)

## 0.81.0 — 2026-09-22

### Added

- **Provide fixed-route credentials APIs with inferred Eden request types and a package-owned manifest integration recipe** (`createCredentialsApi`)

### Changed

- **Delegate configurable credential routes through shared literal-path factories while retaining the same configuration and default endpoints** (`credentialsLogin`, `credentialsRegister`, `credentialsEmailVerification`, `credentialsPasswordReset`)

### Fixed

- **Document the required auth context session binding and protected account route**

## 0.80.1 — 2026-09-22

### Fixed

- **Document verified persistent credentials integration and correct Drizzle and migration APIs**

## 0.80.0 — 2026-09-22

### Added

- **Support existing Drizzle PostgreSQL clients for durable auth sessions** (`createPostgresAuthSessionStore`)

### Fixed

- **Run auth setup according to the selected session store instead of always requiring PostgreSQL**

## 0.79.0 — 2026-09-10

### Breaking

- **Separate API credential token routes from OIDC and reject conflicting configuration** (`apiKeysRoutes`, `auth`, `createAuthApplications`, `AuthConfig`, `ApiKeysConfig`)
  _Migration:_ Send client_credentials requests to /auth/api/token instead of /oauth2/token and update advertised enterprise token URLs. Keep authorization_code and refresh_token requests at /oauth2/token. Prefer auth({ oidc, apikeys }) for configuration validation. API-only consumers may explicitly keep tokenRoute: /oauth2/token if no OIDC handler uses that path.
